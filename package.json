{
  "name": "proxprox",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev:proxy": "bun run --watch src/proxy.ts",
    "dev:client": "bun run --watch src/client.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
