{
  "name": "proxprox",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev:proxy": "bun run --watch src/cli.ts --mode proxy",
    "dev:client": "bun run --watch src/cli.ts --mode client"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
